<%= form_for @product.reviews.new do |f| %>
  <%= f.hidden_field :product_id, value: @product.id %>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit "Сохранить" %>
  </div>
<% end %>
